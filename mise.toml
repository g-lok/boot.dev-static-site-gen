[tools]
poetry = { version = "latest", pyproject = "{{ config_root }}/pyproject.toml" }
python = "3.14.3"
ruff = "latest"

# [tasks.run]
# description = "Run the application"
# run = "poetry run asteroids"
